export interface Product {
    uuid?: string
    name: string
    description: string
    quantity: number
    price: number
    category: string
    sku: string
    createdAt: string
    updatedAt: string
}
